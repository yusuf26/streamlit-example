import streamlit as st
import pandas as pd
import altair as alt
from urllib.error import URLError

st.set_page_config(page_title="Spam Features Extraction")

st.markdown("# Spam Features Extraction with TF-IDF")
st.sidebar.header("Spam Features Extraction")
st.write(
    "Spam Features Extraction"
)
